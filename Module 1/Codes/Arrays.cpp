#include<iostream>
using namespace std;
int main()
{
       /*
       
       int a,b,c,d,e;

        cout<<"Give 5 integers as input"<<endl;
        cin>>a>>b>>c>>d>>e;

        cout<<"The integers in reverse order are: "<<e<<d<<c<<b<<a;



        return 0;
        
        */


/* 
       int  a[4];
       int i;

       cout<<"Give 5 integers as input"<<endl;

       for(i=0;i<5;i++)
       {
        cin>>a[i];
       }

       cout<<"The 5 given input are : "<<endl;

       for(i=0;i<5;i++)
       {
        cout<<a[i]<<" ";
       }

       return 0;

 */
       int  a[9];
       int i=0;

       cout<<"Give 10 integers as input"<<endl;

       for(i;i<10;i++)
       {
        cin>>a[i];
       }

       cout<<"The 10 given input are : "<<endl;

       for(i=9;i>=0;i--)
       {
        cout<<a[i]<<" ";
       }

       return 0;






    /*
    
     int x;      // variable name x of type int --> it can store a single integer which means x can either

    //array can be used to store multiple values i a single variable name

    int a[5]; // This  means it is an array of array of name a which can store multiple integers, it can store 6 integers
                // index starts from 0

        // elements starting from 0 to 5
        
        // the first element will have an index 0 --> a[0]
        // the second element will have an index 1 --> a910
        
        // and goes till [5] 
        
    */

}